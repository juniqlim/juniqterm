# Next Step: 에이전트 코딩 최적화 터미널

## 방향
growTerm을 단순 터미널이 아닌, **에이전트 코딩에 최적화된 터미널**로 발전시킨다.

## 기능 목록

### ~~1. 뽀모도로 타이머~~ ✅
- 터미널에 첫 타이핑 시 25분 타이머 자동 시작
- 타이머 종료 시 터미널 시각적 변화 (색상/오버레이 등)
- 3분간 PTY 입력 차단 → 강제 회고 시간
- 3분 후 자동으로 다음 뽀모도로 시작

### ~~2. 자동 핸드오프~~ ❌

### ~~3. 컨텍스트 사용량 표시~~ 🔒 보류
터미널 상단/하단 바에 컨텍스트 잔여량을 프로그레스 바로 보여준다.
- `/usage` 응답을 파싱하여 표시
- 미해결: 사용자에게 안 보이게 자동 호출하는 방법

### 4-1. Cmd+A 입력 라인 복사 (WIP)
- Cmd+A로 셸 입력 라인을 클립보드에 복사하는 기능 추가 완료
- **미해결**: `❯` 프롬프트 기호 strip이 동작하지 않음 — 실제 셀의 문자가 소스코드의 `❯`(U+276F)와 다른 코드포인트일 가능성. 디버그 로그로 실제 바이트를 확인하여 정확한 문자를 매칭해야 함
- **미해결**: 멀티라인 입력 시 플래시 효과가 첫 줄만 적용됨 — `copy_flash`를 단일 행이 아닌 범위(prompt_row..=bottom)로 변경 필요

### ~~4. 응답 속도 측정/표시~~ ✅

### 5. AI 코칭 (큰 기능)
뽀모도로 종료 시 해당 세션의 사용 패턴을 AI에 질의하여 피드백을 제공한다.
- ~~시작 전 이번 뽀모도로 목표 입력~~ → 나중에
- 25분간의 터미널 사용 패턴 수집 (명령어, 에러, 컨텍스트 클리어 횟수 등)
- AI에 분석 요청 (print 모드 등 비대화형으로)
- 3분 휴식 시간에 코칭 피드백 표시
- ~~목표 대비 달성도 피드백~~ → 목표 입력과 함께 나중에
- 예: "컨텍스트 클리어 2번, 같은 에러 반복. 방향을 먼저 정리해보는 건 어떨까?"

### 6. VT 시퀀스 확장 (터미널 호환성)
Claude Code, vim, htop 등 full-screen TUI 앱이 제대로 동작하려면 아래 지원 필요:

**필수 (full-screen TUI 앱 동작 불가)** ✅
- ~~Alternate Screen Buffer (`CSI ?1049h/l`)~~
- ~~Scroll Region / DECSTBM (`CSI r`) + Insert/Delete Line (`CSI L`, `CSI M`)~~
- ~~누락된 CSI 시퀀스 (커서 절대이동 등)~~

**중요 (없으면 불편하거나 보안/UX 문제)**
- Bracketed Paste Mode (`CSI ?2004h/l`) ✅
- Mouse tracking (`CSI ?1000h` 등) ✅
- Shift+클릭으로 마우스 모드 일시 무시 (터미널 레벨 텍스트 선택)

**나중에 (대부분의 앱이 없어도 동작)** ✅
- ~~Kitty keyboard protocol (`CSI >1u`)~~

---

### ~~7. 메모리 최적화 (현재 ~600MB)~~ ✅
**649MB → 300MB 개선 완료.**
1. ~~Font 객체 중복 → `Arc<fontdue::Font>`로 공유~~ ✅
2. ~~`system_font_cache` 문자별 Font 중복 → 폰트 경로 기준 공유~~ ✅
3. ~~글리프 비트맵 캐시 LRU~~ ❌ 불필요 (수 MB 수준)
4. ~~`visible_cells()` 매 프레임 clone → Cow로 참조 반환~~ ✅

---

### 8. 렌더링 갭 수정
alacritty 비교 분석에서 발견된 항목:

1. **Bold 폰트 미적용** — 색상 변경만 되고 실제 굵은 폰트로 안 그려짐
2. **Italic 무시** — 기울임 표시 없음
3. ~~**Strikethrough 미구현**~~ ✅ (`2d8899a`)
4. **Atlas 패딩 없음** — 글리프 간 텍스처 블리딩 가능성

---

## 기술 포인트
- PTY 입출력 중간에서 가로채기 (growTerm이 이미 PTY 제어 가능)
- 에이전트 응답 완료 시점 감지
- 핸드오프 내용 임시 저장 및 자동 입력
